// import { Component } from 'react';
// import { Route } from 'react-router-dom';
// import routes from '../routes';

// const PublicRoutes = () => {
//   <Route>
//     {routes.map(({ componet: Component, path, exact }) => (
//       <Route path={`/${path}`} key={path} exact={exact}>
//         <Component />
//       </Route>
//     ))}
//   </Route>;
// };
// export default PublicRoutes;
